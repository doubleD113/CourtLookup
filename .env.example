# Neon PostgreSQL — pooled connection string (used by app at runtime)
DATABASE_URL=""

# Neon PostgreSQL — direct/unpooled connection string (used by Prisma migrations)
DATABASE_URL_UNPOOLED=""

# Google Maps JS API key (public — safe to expose in browser)
NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=""

# Google Maps Map ID (required for AdvancedMarker)
NEXT_PUBLIC_GOOGLE_MAPS_MAP_ID=""

# Google Places API key (server-side only — never expose publicly)
GOOGLE_PLACES_API_KEY=""
